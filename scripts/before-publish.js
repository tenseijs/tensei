const Fs = require('fs')
const Path = require('path')

// Loop through all templates and update their package.json versions to an upper version

// Update cli generator package version to next version
