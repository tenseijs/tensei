import React from 'react'

class HasMany extends React.Component {
    render() {
        const {
            value,
            onFieldChange,
            field,
            errorMessage,
            ...rest
        } = this.props

        return null
    }
}

export default HasMany
