# Multisites
=================

```
tensei()
    .resources([...])
    .sites([
        site('Course Platform')
            .resources([...])
            .routes([...])
            .graphQlServers([...])
    ])
```
